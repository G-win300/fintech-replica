apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: notifications-transactions
  namespace: kuda
spec:
  pubsubname: kuda-pubsub
  topic: transactions
  routes:
    default: /events/transaction-created
scopes:
  - notifications-service
---
apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: ledger-transactions
  namespace: kuda
spec:
  pubsubname: kuda-pubsub
  topic: transactions
  routes:
    default: /events/transaction-created
scopes:
  - ledger-service